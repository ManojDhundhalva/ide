1. folder is expand or not, make it socket and send to backend/workspace and store in redis and then if user closes then connection then store it to mongodb.

2. add redisSet name "file-explorer" => FILE_EXPLORER_SET